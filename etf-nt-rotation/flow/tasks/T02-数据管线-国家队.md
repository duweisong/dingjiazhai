# T02 · 数据管线-国家队（预测仓位信号）

- **背景**（是什么问题）：策略需要国家队仓位信号来调节 ETF 轮动仓位比例。已有 `nt-position-sizer` 预测引擎和 `national_team_tracker.py` 数据拉取工具，需要验证整条链路能跑通，并封装为策略可消费的接口。
- **目标**（解决什么问题）：
  1. 验证 `national_team_tracker.py` → `predictive_engine.py` → `predicted_position.json` 链路
  2. 写 `scripts/fetch_nt_data.py` 一键脚本（拉取→计算→预测→校验）
  3. 产出集成测试，确保回测脚本可直接调用
- **输入**（依赖）：
  - `docs/specs/strategy-design.md` §2.2（预测模型架构）
  - `../national_team_tracker.py`（季度数据拉取）
  - `../nt-position-sizer/src/predictive_engine.py`（预测引擎）
  - `../nt-position-sizer/src/position_engine.py`（实际仓位引擎）
- **产出**（交付什么）：
  - `scripts/fetch_nt_data.py` — 一键数据管线脚本
  - 验证报告（链路状态 + 数据完整性）
- **验收标准**（怎么算做完做对）：
  - [ ] 链路三环节（拉取→计算→预测）全部跑通或明确阻塞点
  - [ ] `predicted_position.json` 可成功读取，字段完整
  - [ ] 仓位值在 [0.02, 0.98] 范围内
  - [ ] 数据缓存目录 `.cache/national_team/` 结构已确认
  - [ ] fetch_nt_data.py 含错误退出码和完整性校验
- **状态**：完成
